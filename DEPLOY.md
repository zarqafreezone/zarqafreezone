# 🚀 دليل نشر تطبيق المنطقة الحرة الزرقاء

هذا الدليل يشرح كيف تنشر التطبيق مجاناً وتحصل على **رابط HTTPS دائم** يعمل من أي هاتف حول العالم.

> المنصة الموصى بها: **Render** (مجانية، موثوقة، تدعم Node وملفات ثابتة).

---

## ✅ المشروع جاهز للنشر
- الخادم يقرأ المنفذ من متغيّر `PORT` ويربط على `0.0.0.0` (متطلب الاستضافات).
- لا توجد مكتبات خارجية (لا حاجة لانتظار `npm install`).
- يوجد ملف `render.yaml` (نشر بضغطة زر) و `.gitignore`.
- نقطة فحص الصحة: `/api/health`.

---

## 📤 الطريقة: من GitHub إلى Render (الأفضل)

### الخطوة 1 — ارفع الكود إلى GitHub
1. أنشئ حساباً مجانياً على [github.com](https://github.com) إن لم يكن لديك.
2. اضغط **＋ New repository** → سمّه `zarqa-free-zone` → **Public** → أنشئ.
3. افتح المستودع الجديد → **uploading an existing file** (رابط "upload") .
4. **فك ضغط الأرشيف** `zarqa-free-zone-backup.zip` على حاسوبك.
5. اسحب **كل الملفات والمجلدات** إلى صفحة GitHub → اكتب رسالة → **Commit changes**.

### الخطوة 2 — أنشئ حساب Render واربطه
1. ادخل [render.com](https://render.com) وسجّل **باستخدام حساب GitHub**.
2. من اللوحة: **New +** → **Web Service**.
3. اختر مستودع `zarqa-free-zone` الذي رفعته → **Connect**.

### الخطوة 3 — اضبط الإعدادات
| الإعداد | القيمة |
|---|---|
| Name | `zarqa-free-zone` |
| Runtime | **Node** |
| Build Command | `npm install` |
| Start Command | `node server.js` |
| Instance Type | **Free** |

ثم في قسم **Environment Variables** أضِف:
- `ADMIN_PASSWORD` = كلمة مرور قوية تختارها (بدلاً من admin123)
- (اختياري لاحقاً) `STRIPE_PUBLISHABLE_KEY` و `STRIPE_SECRET_KEY`

### الخطوة 4 — انشر!
- اضغط **Create Web Service**.
- انتظر دقيقتين تقريباً حتى ينتهي البناء (Build).
- عند ظهور **Live** ستحصل على رابط مثل:
  `https://zarqa-free-zone.onrender.com` 🎉

افتحه من هاتفك — التطبيق يعمل! ويمكنك بعدها تثبيته كـ PWA.

---

## ⚠️ ملاحظة مهمة حول البيانات (اقرأها)
النسخة الحالية تستخدم ملف `db.json` لتخزين البيانات. على **Render Free** يبقى
القرص أثناء عمل الخدمة، لكنه **يُعاد ضبطه عند كل إعادة نشر** (تُحفظ الإعدادات الأصلية فقط).
- للاستخدام الجدي بطاولات مستخدمين وإعلانات حقيقية دائمة → **الخطوة التالية الموصى بها**
  هي ربط قاعدة بيانات حقيقية مجانية (مثل **Neon Postgres** أو **Supabase**).
- أنا جاهز لأضيف هذا لك في الخطوة التالية بعد النشر.

---

## 🔁 خطوات بديلة (أبسط لكن بقيود)
- **Glitch** ([glitch.com](https://glitch.com)): New Project → Import from GitHub → يعمل فوراً برابط.
- **Railway** ([railway.app](https://railway.app)): ربط GitHub → Deploy Node (تجربة مجانية محدودة).

---

## 🧪 بعد النشر: تحقّق
افتح رابطك ثم:
1. `/api/health` → يجب أن يرجع `{"ok":true,...}`
2. جرّب تسجيل الدخول وإضافة إعلان.
3. ادخل لوحة الإدارة بكلمة المرور التي عيّنتها.

---

نشره: **شبكة نشوان للتسويق** 🤝
